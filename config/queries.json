{
  "login": "SELECT * FROM usuarios WHERE username = $1 and password = $2",
  "checkUser": "SELECT * FROM usuarios WHERE username = $1",
  "register": "INSERT INTO usuarios(username, password) VALUES($1, $2)"
}
